import { BaseFilter } from "./baseFilter";
import { LigneAccueille } from "./ligneAccueil";

export class LigneAccueilleFilter
{
    filter : 
    {
        id : BaseFilter;
        name : BaseFilter;
        name2 : BaseFilter;
        text : BaseFilter;
        text2 : BaseFilter;
        image : BaseFilter;
        BaseFilter1 : BaseFilter;
        type_content : BaseFilter;
        ordre : BaseFilter;
        is_deleted : BaseFilter;
        id_article : BaseFilter;
        id_categorie : BaseFilter;
        facebook : BaseFilter;
        linkedin : BaseFilter;
        id_accueil :BaseFilter;
        id_parent : BaseFilter;
    }
}